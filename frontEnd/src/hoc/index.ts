// hoc文件夹内，如果简单就是单文件，如果复杂，就是文件夹，统一通过本文件透出即可
import { Check, IvaliRenderProps, Vali } from './Validate' // 不带antdForm,纯数据处理
import { CheckForm, CheckFormItem, IcheckFormRenderProps } from './ValidateForm' // 带antdform

export { CheckForm, CheckFormItem, IcheckFormRenderProps, Check, IvaliRenderProps, Vali }
